micronaut:
  application:
    name: consulta-renda-fixa
    server:
      port: 8081
  router:
    static-resources:
      swagger:
        paths: classpath:META-INF/swagger
        mapping: /swagger/**

endpoints:
  routes:
    sensitive: false

netty:
  default:
    allocator:
      max-order: 3